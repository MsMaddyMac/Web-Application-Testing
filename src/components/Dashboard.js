import React from 'react';

import Button from './Button';

const Dashboard = () => {
    return (
        <div>
            <Button label={'Strike'}/>
            <Button label={'Ball'}/>
            <Button label={'Foul'}/>
            <Button label={'Hit'}/>
        </div>
    )
}

export default Dashboard
